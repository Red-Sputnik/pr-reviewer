# PR Reviewer Service

Сервис для автоматического назначения ревьюеров на Pull Request’ы внутри команды.
Разработан как тестовое задание для стажировки Backend (Golang).

## 🧩 Основной функционал

* CRUD для пользователей (User)
* CRUD для команд (Team)
* Автоматическое назначение ревьюеров по правилам:

  * исключение автора PR
  * исключение неактивных пользователей
  * поддержка минимального числа ревьюеров
* Поддержка PostgreSQL
* Встроенная миграция
* REST API
* Docker + Docker Compose

---

## 📁 Структура проекта

```
.
├── cmd/
│   └── server/main.go
├── internal/
│   ├── handlers/      # HTTP-обработчики
│   ├── middleware/    # Middleware
│   ├── models/        # Бизнес-модели User, Team
│   ├── repository/    # Работа с БД
│   ├── service/       # Бизнес-логика
├── migrations/        # SQL-миграции
├── static/
├── templates/
├── Dockerfile
├── docker-compose.yml
├── .env
└── README.md
```

---

## 🚀 Запуск проекта

### **1. Клонировать репозиторий**

```bash
git clone https://github.com/<your-name>/<your-repo>.git
cd <your-repo>
```

---

### **2. Настроить переменные окружения**

Файл **.env** уже включён:

```
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=pr_reviewer

APP_PORT=8080
```

---

### **3. Запуск через Docker Compose**

```bash
docker-compose up --build
```

После запуска приложение доступно по адресу:

**[http://localhost:8080](http://localhost:8080)**

---

## 🐳 Docker

### Dockerfile

* Собирает Go-приложение
* Использует multi-stage build
* Минимальный итоговый размер образа

### docker-compose.yml

Поднимает:

* `app` — Go backend
* `db` — PostgreSQL
* Автоматические миграции при старте

---

## 📬 API Endpoints

Примеры:

### **Создать команду**

```
POST /teams
```

### **Получить список команд**

```
GET /teams
```

### **Создать пользователя**

```
POST /users
```

### **Назначить ревьюеров**

```
POST /reviewers/assign
```

---


## 🛠 Используемые технологии

* Go 1.22+
* PostgreSQL 14+
* Chi Router
* Docker / Docker Compose
* SQL миграции

---

## 👨‍💻 Автор

https://github.com/Red-Sputnik

---
